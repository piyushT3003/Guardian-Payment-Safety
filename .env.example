# Optional: override the default local/demo webhook secret
RAZORPAY_WEBHOOK_SECRET=guardian_demo_secret
# Optional: use a persistent writable path on a hosted platform
# GUARDIAN_DB_PATH=/path/to/guardian.db
